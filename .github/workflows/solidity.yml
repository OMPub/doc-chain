name: Solidity

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  foundry:
    name: Foundry
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.7.0

      - name: Show Forge version
        run: forge --version

      - name: Check formatting
        run: make fmt-check

      - name: Build
        run: forge build --sizes

      - name: Test
        run: forge test -vvv

      - name: Check committed ABI
        run: make abi-check-forge

  slither:
    name: Slither
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      # crytic-compile auto-detects the root foundry.toml and shells out to
      # `forge config`, which is not installed in the slither-action container,
      # crashing before analysis even with --compile-force-framework solc. Hide
      # it so Slither compiles DocChain.sol directly with solc (the contract has
      # no imports). The Foundry job uses its own checkout and is unaffected.
      - name: Hide Foundry config so Slither uses solc
        run: mv foundry.toml foundry.toml.disabled

      - name: Run Slither
        uses: crytic/slither-action@v0.4.2
        with:
          target: contracts/src/DocChain.sol
          slither-config: slither.config.json
          solc-version: "0.8.35"
          slither-args: --compile-force-framework solc --solc-args "--base-path . --evm-version paris --optimize" --exclude timestamp
